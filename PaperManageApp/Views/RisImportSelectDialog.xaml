<Window x:Class="PaperManagementApp.Views.RisImportSelectDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PaperManagementApp.Views"
        mc:Ignorable="d"
        Title="論文選択" Height="450" Width="700"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- ヘッダー -->
        <TextBlock Grid.Row="0" 
                   Text="RISファイルから複数の論文が見つかりました。インポートする論文を選択してください。" 
                   TextWrapping="Wrap" 
                   Margin="0,0,0,15"/>

        <!-- 論文リスト -->
        <ListView Grid.Row="1" 
                  x:Name="PapersListView" 
                  SelectionMode="Single"
                  SelectionChanged="PapersListView_SelectionChanged"
                  Margin="0,0,0,15">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="タイトル" Width="300" DisplayMemberBinding="{Binding Title}"/>
                    <GridViewColumn Header="著者" Width="150" DisplayMemberBinding="{Binding Authors}"/>
                    <GridViewColumn Header="出版年" Width="60" DisplayMemberBinding="{Binding Year}"/>
                    <GridViewColumn Header="雑誌" Width="150" DisplayMemberBinding="{Binding Journal}"/>
                </GridView>
            </ListView.View>
        </ListView>

        <!-- ボタン -->
        <StackPanel Grid.Row="2" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right">
            <Button Content="キャンセル" 
                    IsCancel="True" 
                    Width="100" 
                    Margin="0,0,10,0"
                    Padding="0,5"/>
            <Button Content="選択" 
                    IsDefault="True" 
                    Width="100" 
                    Click="SelectButton_Click"
                    x:Name="SelectButton"
                    IsEnabled="False"
                    Padding="0,5"/>
        </StackPanel>
    </Grid>
</Window>