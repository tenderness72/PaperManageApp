<Page x:Class="PaperManagementApp.Views.PaperListView"
     xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
     xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
     xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
     xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
     xmlns:local="clr-namespace:PaperManagementApp.Views"
     mc:Ignorable="d"
     d:DesignHeight="650" d:DesignWidth="900"
     Title="論文一覧">

    <Grid Background="Transparent"
          AllowDrop="True"
          DragOver="RootGrid_DragOver"
          DragEnter="RootGrid_DragEnter"
          DragLeave="RootGrid_DragLeave"
          Drop="RootGrid_Drop">
        <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- ヘッダー部分 -->
        <Grid Grid.Row="0" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Text="論文一覧"
                       FontSize="24"
                       FontWeight="Bold"
                       VerticalAlignment="Center"/>

            <Button Grid.Column="1"
                    Content="新規追加"
                    Click="AddButton_Click"
                    Padding="15,5"
                    Background="#27ae60"
                    Foreground="White"
                    BorderThickness="0"/>
        </Grid>

        <!-- 一括操作バー -->
        <Border Grid.Row="1" Margin="0,0,0,8" Padding="10,6"
                Background="#fef9e7" BorderBrush="#f39c12" BorderThickness="1" CornerRadius="4">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <CheckBox x:Name="SelectAllCheckBox"
                          Click="SelectAllCheckBox_Click"
                          VerticalAlignment="Center"
                          Margin="0,0,10,0"
                          ToolTip="すべて選択 / 解除"/>
                <TextBlock x:Name="SelectionCountText"
                           Text="0件選択中"
                           VerticalAlignment="Center"
                           Foreground="#666"
                           Margin="0,0,15,0"/>
                <Button x:Name="BulkDeleteButton"
                        Content="削除"
                        Click="BulkDeleteButton_Click"
                        IsEnabled="False"
                        Padding="12,4"
                        Margin="0,0,6,0"
                        Background="#e74c3c"
                        Foreground="White"
                        BorderThickness="0"/>
                <Button x:Name="BulkFavoriteButton"
                        Content="お気に入り切替"
                        Click="BulkFavoriteButton_Click"
                        IsEnabled="False"
                        Padding="12,4"
                        Background="#f39c12"
                        Foreground="White"
                        BorderThickness="0"/>
            </StackPanel>
        </Border>

        <!-- 論文リスト -->
        <DataGrid x:Name="PapersDataGrid"
                  Grid.Row="2"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  SelectionMode="Single"
                  SelectionChanged="PapersDataGrid_SelectionChanged"
                  MouseDoubleClick="PapersDataGrid_MouseDoubleClick"
                  GridLinesVisibility="Horizontal"
                  BorderThickness="1"
                  BorderBrush="#e0e0e0"
                  RowHeaderWidth="0"
                  Background="White"
                  AlternatingRowBackground="#f9f9f9"
                  AllowDrop="False">

            <DataGrid.Columns>
                <!-- 選択チェックボックス列 -->
                <DataGridTemplateColumn Width="35">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <CheckBox IsChecked="{Binding IsSelected, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                      Click="SelectionCheckBox_Click"
                                      HorizontalAlignment="Center"
                                      VerticalAlignment="Center"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <!-- お気に入りインジケータ（クリックで個別切替） -->
                <DataGridTemplateColumn Width="35">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Click="StarButton_Click"
                                    Tag="{Binding Id}"
                                    Background="Transparent"
                                    BorderThickness="0"
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center"
                                    Cursor="Hand"
                                    ToolTip="お気に入り切替">
                                <TextBlock FontSize="15" VerticalAlignment="Center">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Text" Value="☆"/>
                                            <Setter Property="Foreground" Value="#ccc"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsFavorite}" Value="True">
                                                    <Setter Property="Text" Value="★"/>
                                                    <Setter Property="Foreground" Value="#f39c12"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </Button>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTextColumn Header="タイトル"
                                    Binding="{Binding Title}"
                                    Width="2*"/>

                <DataGridTextColumn Header="著者"
                                    Binding="{Binding Authors}"
                                    Width="1*"/>

                <DataGridTextColumn Header="年"
                                    Binding="{Binding Year}"
                                    Width="50"/>

                <DataGridTextColumn Header="ジャーナル"
                                    Binding="{Binding Journal}"
                                    Width="1*"/>

                <DataGridTextColumn Header="巻・ページ"
                                    Binding="{Binding Volume}"
                                    Width="80"/>

                <DataGridTemplateColumn Width="100">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <Button Content="編集"
                                        Click="EditButton_Click"
                                        Tag="{Binding Id}"
                                        Margin="0,0,5,0"
                                        Padding="5,2"
                                        Background="#3498db"
                                        Foreground="White"
                                        BorderThickness="0"/>

                                <Button Content="削除"
                                        Click="DeleteButton_Click"
                                        Tag="{Binding Id}"
                                        Padding="5,2"
                                        Background="#e74c3c"
                                        Foreground="White"
                                        BorderThickness="0"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>

            <DataGrid.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="詳細を表示" Click="ViewDetailMenuItem_Click"/>
                    <MenuItem Header="編集" Click="EditMenuItem_Click"/>
                    <MenuItem Header="Wordに引用を挿入" Click="InsertCitationMenuItem_Click"/>
                    <MenuItem Header="削除" Click="DeleteMenuItem_Click"/>
                </ContextMenu>
            </DataGrid.ContextMenu>
        </DataGrid>
        </Grid>

        <!-- PDFドラッグ中オーバーレイ -->
        <Grid x:Name="DragOverlay"
              Visibility="Collapsed"
              Panel.ZIndex="999"
              Background="#CC1a252f">

            <StackPanel x:Name="OverlayIcon"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        RenderTransformOrigin="0.5,0.5">
                <StackPanel.RenderTransform>
                    <ScaleTransform ScaleX="1" ScaleY="1"/>
                </StackPanel.RenderTransform>

                <!-- 論文アイコン（角折れ書類） -->
                <Viewbox Width="120" Height="120" Margin="0,0,0,20">
                    <Canvas Width="100" Height="120">
                        <!-- 書類本体 -->
                        <Path Fill="White" Opacity="0.95"
                              Data="M10,0 L70,0 L100,30 L100,120 L10,120 Z"/>
                        <!-- 折り返し -->
                        <Path Fill="#aab8c2"
                              Data="M70,0 L70,30 L100,30 Z"/>
                        <!-- 文字線 -->
                        <Line X1="22" Y1="50" X2="78" Y2="50" Stroke="#3498db" StrokeThickness="5" StrokeStartLineCap="Round" StrokeEndLineCap="Round"/>
                        <Line X1="22" Y1="65" X2="78" Y2="65" Stroke="#3498db" StrokeThickness="5" StrokeStartLineCap="Round" StrokeEndLineCap="Round"/>
                        <Line X1="22" Y1="80" X2="55" Y2="80" Stroke="#3498db" StrokeThickness="5" StrokeStartLineCap="Round" StrokeEndLineCap="Round"/>
                        <!-- ＋バッジ -->
                        <Ellipse Canvas.Left="55" Canvas.Top="75" Width="44" Height="44" Fill="#2ecc71"/>
                        <TextBlock Canvas.Left="64" Canvas.Top="78"
                                   Text="+" FontSize="28" FontWeight="Bold"
                                   Foreground="White" TextAlignment="Center"/>
                    </Canvas>
                </Viewbox>

                <TextBlock Text="PDFをドロップして追加"
                           FontSize="22"
                           FontWeight="Bold"
                           Foreground="White"
                           HorizontalAlignment="Center"/>
                <TextBlock Text=".pdf ファイルを放してください"
                           FontSize="14"
                           Foreground="#aab8c2"
                           HorizontalAlignment="Center"
                           Margin="0,8,0,0"/>
            </StackPanel>
        </Grid>
    </Grid>
</Page>
